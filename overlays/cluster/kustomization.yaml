apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - tempomonolithic.yaml
  - namespace-tempo.yaml
  - rules/tempo-rbac.yaml
labels:
  - pairs:
      app.kubernetes.io/managed-by: argocd
      app.kubernetes.io/part-of: tempo-operator
